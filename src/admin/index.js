/**
 * Admin panel entry point.
 *
 * All admin pages and their routes are exported from this folder.
 * Import this file in App.js (or wherever your routes are defined) to
 * register all admin routes at once.
 *
 * Usage in App.js:
 *   import adminRoutes from './admin';
 *   // then map adminRoutes inside your <Routes> element (same as src/routes/adminRoutes.js)
 */
export { default } from '../routes/adminRoutes';
